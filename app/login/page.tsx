import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
  title: "Login - ConnectPay",
  description: "Login to your ConnectPay account",
};

export default function LoginPage() {
  return <LoginForm />;
}